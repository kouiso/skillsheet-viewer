# Dogfooding 実施結果（2026-08-04）

`doc/dogfooding-test-spec.md` に沿って全画面を実際に操作した結果。

## 実施環境

| 項目 | 値 |
|---|---|
| ビルド | `pnpm build` → `pnpm start -p 3210`（本番ビルド） |
| DB | Neon 本番 `skillsheet-viewer` からのコピーブランチ `dogfooding-20260804`（本番へは書き込みなし） |
| データ | 実データ 1 シート / 19 社 / 32 案件 / スキル 34 件 |
| ブラウザ | Chromium 1194（Playwright 1.62） |
| ビューポート | 320×800 / 375×812 / 768×1024 / 1280×800 |
| テーマ | light / dark |
| 証跡 | `test-results/dogfooding/`（1 巡目）/ `round2/`（2 巡目）/ `round3/`（3 巡目）/ `round4/`（4 巡目）。スクリーンショット・描画テキスト・PDF のラスタライズ |

検証は 160 件の一次指摘を挙げたうえで、1 件ずつ「反証する側」で再検証し、
仕様どおりの挙動・元データ側の記述揺れ・測定条件の誤りに起因するもの 100 件を棄却した。
残った 60 件を重複統合し、2〜3 巡目で見つけた 2 件を足して 29 件の逸脱にまとめている。

1 巡目で未実施だったケースは 2 巡目で、レビューで「サブステップが未検証」と指摘された分は
3 巡目と 4 巡目で実際に動かした。残る `BLOCKED` は markdown シート（`/view/[path]`）の描画に依存する
**B-10 / B-13b / B-15b の 3 ケース**で、これはこの環境の `GITHUB_TOKEN` がプロキシ用
プレースホルダで実トークンを用意できないことによる。

このほか、`/view/db` の DB 取得失敗フォールバック（E-4 の `console.error` 分岐と
E-5 のフォールバック UI）が **未実施**。画面自体は E-1 の 11 ルートに含めて全ビューポート・
両テーマで撮っているが、DB が正常だったため異常系の分岐だけ通っていない。

## 元データとの件数突合

| 項目 | 元データ `skillsheet.md` | 正本 DB | 画面 |
|---|---|---|---|
| 会社 | 19 | 19 | 19 |
| 案件 | 32 | 32 | 32 |
| スキル | 46（8 カテゴリ） | 34（6 カテゴリ） | 34（6 カテゴリ） |

会社数・案件数は完全一致。技術スタックも全 32 案件で要素数まで一致している。
スキルのみ 12 件・2 カテゴリが欠落（D-1）。

## ケース別結果

4 巡している。1 巡目で未実施だったケースは 2 巡目（`round2/`）で、レビューで「サブステップが未検証」と指摘された分は 3 巡目（`round3/`）と 4 巡目（`round4/`）で実際に動かした。

4 巡目時点の DB は 2 巡目の C-8（hidden 検証）で会社「個人開発」を非表示にしたまま残っているため、案件カウンタが 32 件ではなく **30 件**を示す。これは検証用ブランチに対する意図的な操作の残りで、データ欠落ではない。

| ID | 結果 | 備考 |
|---|---|---|
| A-1 未認証で `/view` | PASS | `/viewer-auth` へリダイレクト（`next` は付かない＝現行仕様どおり）。元の遷移先が失われる件は U-6 |
| A-2 閲覧コード誤り | **FAIL** | 画面は壊れないが、エラー文言のコントラストが 3.88:1 で AA 未達（S-4 / #152）。共通 PASS 条件にコントラストを含めている以上 FAIL |
| A-3 閲覧コード正 | PASS | `?next=` を明示したときは指定パスへ復帰する |
| A-4 ログイン誤り | **FAIL** | 文言は想定どおりだが A-2 と同じエラーバナーのコントラスト（S-4 / #152） |
| A-5 ログイン正 | PASS | `/builder` へ遷移 |
| A-6 open redirect | PASS | `next=//evil.example.com` でも `/builder` に留まる |
| A-7 ログアウト | PASS | 閲覧コードだけで認証した独立コンテキストで実行。`POST /api/logout` が 200、その後 `/view` へ行くと `/viewer-auth` に戻される。編集者セッションを持ったまま実行するとゲートは効かない（`requireViewer()` が `isEditor()` でも通すため）ので、この前提を外さないこと |
| B-1 シート一覧 | PASS | 件数・タイトル・更新日が DB と一致。検索で 0 件にすると「シートが見つかりません」が出る |
| B-2 ダッシュボード初期表示 | **FAIL** | 320px で横スクロール（L-1 / #143） |
| B-3 profile 突合 | **FAIL** | 所属会社・性別・資格・得意分野・得意業務が画面に出ず、自己PR も大幅短縮（D-2 / #140）。所属会社だけは保存先・描画とも実装済みで取り込みが値を入れていないだけ |
| B-4 stats 突合 | **FAIL** | 4 枠の値と単位そのものは元データと矛盾しない。ただし統計カード 4 枚目が直上メタ行と重複しており（S-3 / #152）、共通判定の「BUG が 1 件でもあれば FAIL」に従って FAIL とする |
| B-5 skills 突合 | **FAIL** | 46 → 34 件へ欠落（D-1 / #137）、習熟度が画面に出ない（D-8 / #142） |
| B-6 案件件数突合 | PASS | 19 社 / 32 案件が元データと一致 |
| B-7 案件詳細突合 | **FAIL** | D-3 / D-4 / D-5 / D-6 / D-7（#139 / #138 / #141 / #136 / #151） |
| B-8 期間表記 | PASS | `2025.11〜現在` 形式が 64 件描画され、継続中案件は「継続中」と出る。元データの期間と一致 |
| B-9 ビュートグル | PASS | 4 ビューの ON/OFF が効く |
| B-10 目次（markdown 経路） | **BLOCKED** | markdown シートを描画できず（B-15）。ダッシュボードは仕様上の対象外 |
| B-11 技術フィルタ | **FAIL** | 空欄プレースホルダ「-」が 1 技術として集計（D-5 / #141）。0 件時の表示は 4 巡目で実測: 検索欄に該当なしの語を入れるとカウンタが `0 / 30 件` になり「条件に一致する案件がありません」が出る。技術チップとの併用でも同じ。「クリア」で `30 / 30 件` に戻る。なお技術チップは OR 条件（`project-section.tsx:77`）なので、**チップの選択だけでは 0 件にならず**、0 件の再現には検索欄が要る |
| B-12 工程ステッパー | **FAIL** | 狭幅でラベルが分裂（L-2 / #144）、ドーナツが満円に見える（S-1 / #152） |
| B-13a Markdown 描画（構造化ブロック） | **FAIL** | 案件テキストが Markdown 未解釈（D-4 / #138） |
| B-13b Markdown 描画（GFM 表 / `<details>`） | **BLOCKED** | markdown シートを描画できず（B-15） |
| B-14 存在しない ID | **FAIL** | Next.js 既定の英語 404（U-1 / #148） |
| B-15a `/view/[path]`（env 未設定時の案内） | **FAIL** | env を外して実行。500 にはならないが「時間をおいて再度お試しください」と出て原因に触れない。同条件で `/compare` は「GitHub 連携が未設定のため…」と正しく案内する（U-7 / #157） |
| B-15b `/view/[path]`（markdown シートの描画） | **BLOCKED** | env を設定して実行したが GitHub API が 401。この環境の `GITHUB_TOKEN` は 14 文字のプロキシ用プレースホルダで、実トークンを用意できない |
| B-16 `/compare` | **FAIL** | UI から到達不能なうえ、経路自体が DB シートを比較できない（U-2 / #145） |
| C-1 builder 初期表示 | PASS | 19 社 / 32 案件が正しく出る |
| C-2 ブロック追加 | PASS | テキスト / テーブル / スキル一覧 / 職務経歴 を 1 種ずつ追加 → 保存 → 同じシートを `?sheet=<id>` で開き直して DB のブロック数と突合（4 巡目）。**テーブルと スキル一覧 は永続化される**（DB 8→9、9→10）。テキストと職務経歴は空のまま保存すると DB に入らず、リロードで消える。これは `blocks.ts:373` の `isBlockInputEmpty` による意図的な drop（「保存時の drop と、全消し保存ガードで共有する単一の真実」）で、内容を入れれば残る。詳細は「棄却した主な指摘」参照 |
| C-3 並べ替え | PASS | 並べ替えハンドル 9 個。ドラッグで順序変更を実行できた |
| C-4 ブロック削除 | PASS | 削除 → 保存 → 反映を確認 |
| C-5 自動保存 | PASS | 通常時は「保存済み（自動）」。オフラインにすると「自動保存に失敗 — 保存ボタンで再試行」、復帰後に再編集すると「保存済み（自動）」に戻る |
| C-6 手動保存 | PASS | DB 反映を確認 |
| C-7 案件エディタ | PASS | 会社追加・案件追加・hidden トグルに加え、技術タグ入力（`aria-label="使用言語"` にタグを追加して 1 → 2 件）と工程選択（`aria-pressed` が切り替わる）も動作 |
| C-8 hidden 反映 | PASS | 案件ナビの目玉ボタン 21 個。会社「個人開発」を非表示にすると、ビューアの案件件数が 32 → 30 に減る |
| C-9 履歴 | PASS | 変更履歴ドロワーに操作が時系列で積まれる。2 件以上になると「この時点に戻す」が出て、押すと確認ダイアログを経て復元できる（先頭は「いまの状態」なので復元対象にならないのは設計どおり） |
| C-10 新規シート作成 | PASS | テンプレート「フルスキルシート」を選んで作成すると、初期ブロックが 5 個入った状態で開く |
| C-11 シート削除 | PASS | 削除できる |
| C-12a バックアップ書き出し（hidden 保持） | PASS | hidden にした案件（HorseManager）がバックアップに残る＝復元で失われない |
| C-12b バックアップ書き出し（全データ） | **FAIL** | 会社概要文（`CompanyInfo.note`）が出力されない。「業務委託にて」で検索して 0 件（D-3 / #139） |
| C-13 離脱警告 | PASS | 未保存状態で `beforeunload` の `preventDefault` が効く |
| C-14a 狭幅ナビ | PASS | 320 / 375px の初期表示でナビがレールに畳まれ、ハンバーガーで開閉できる。ページの横スクロールなし |
| C-14b 狭幅のスキル表 | **FAIL** | 320px でスキル表が潰れて経験年数が読めない（L-4 / #150） |
| C-15a 別窓プレビュー（同期） | PASS | 編集内容が別窓に反映される |
| C-15b 別窓プレビュー（直接アクセス） | **FAIL** | 未接続なのに「編集画面が閉じられました」と出る（U-5 / #151） |
| D-1 PDF ダウンロード | PASS | 19 ページ / 267KB |
| D-2 PDF 内容突合 | **FAIL** | 画面と PDF で情報量が違う（D-4 / #138、D-8 / #142）。元データからの欠落（D-1 / #137）も PDF 側に同じく波及している |
| D-3 ページ切れ | **FAIL** | 見出しと本体が改ページで分断（P-2 / #147） |
| D-4 日本語フォント | **FAIL** | 豆腐は無いが不正なハイフンが入る（P-1 / #146） |
| D-5 hidden 除外 | PASS | 会社「個人開発」を非表示にすると PDF が 19 → 18 ページになり、HorseManager / HorseFeeders / 個人開発 の出現回数がいずれも 0 になる |
| E-1 全画面 × 4VP × 2テーマ | **FAIL** | **11 ルート** × 4 ビューポート × 2 テーマを撮影。320px の横スクロール（L-1 / #143）を検出。1〜3 巡目の 8 ルートは `/view` / `/view/db` / `/view/db/:id` / `/view/db/<存在しない UUID>` / `/builder` / `/builder/preview` / `/compare` / 404（`/this-route-does-not-exist`）。4 巡目で `/login` / `/viewer-auth` / `/view/[path]` を追加撮影し、`/login` と `/viewer-auth` は全 8 通りで横スクロールなし・`console.error` ゼロ、`/view/[path]` は全 8 通りで **HTTP 500 + `console.error` 3 件**（U-7 / #157 と同根）。`/view/db` の証跡は 1 巡目のハーネスが `B-15-*` の名前で保存しており、ファイル名とケース ID が食い違っている |
| E-2 dark テーマ | PASS | 全画面で文字は読める。面・境界のコントラストは「棄却した主な指摘」のとおり意図的なトークン設計 |
| E-3 キーボード操作 | **FAIL** | Tab で 12 要素に到達できるが、フォーカスリングが出るのは 3 要素だけ（S-6 / #156） |
| E-4 console.error | **FAIL** | DB 経路の全画面（`/view` / `/view/db` / `/view/db/:id`）と `/login` / `/viewer-auth` はゼロ。`/view/[path]` は 4 ビューポート × 2 テーマの全 8 通りで **HTTP 500 かつ `console.error` 3 件**（`error.tsx` の `console.error('Route error boundary:', err)` を含む。U-7 / #157 と同根）。なお `/view/db` は DB 取得に失敗したときだけ `console.error('Failed to load DB skill sheet:', err)` を出す分岐を持つが、今回は DB が正常だったため**この分岐は未実行**（下記参照） |
| E-5 エラー画面 | **FAIL** | U-1。加えて `/view/db` の DB 失敗フォールバック UI（「DB版スキルシートを表示できません」）は **未実施**。`DATABASE_URL` / `SKILLSHEET_OWNER_ID` を外すかテーブルを落とさないと再現しないため、次回はこの条件を作って確認する |

## 逸脱一覧

親 Issue: #135

逸脱 **29 件**を子 Issue **21 本**（#136〜#153 / #155〜#157）として起票している。
単独では着手単位にならない小さな逸脱はまとめてあるため、逸脱の数と Issue の数は一致しない
（まとめた Issue は本文で逸脱ごとに節を分け、完了条件もチェックボックスを分けている）。

> ID の読み方: 上の「ケース別結果」の `D-1`〜`D-5` は仕様書 D 章（PDF）のテストケース、
> ここの `D-1`〜`D-8` は「データ」分類の逸脱で、別の ID 空間。
> ケース表から逸脱を参照している箇所は Issue 番号を併記して区別できるようにしている。

### データ（元データ → 画面の欠落・誤り）

| ID | 内容 | 主な箇所 |
|---|---|---|
| D-1 (#137) | スキルが元データ 46 件中 34 件しか出ない。カテゴリ「CMS」「テスト」が丸ごと消失。欠落 12 件は Java / WordPress / Expo / GraphQL / Sanity CMS / Prisma / CakePHP / Vitest / Playwright / Jest / Turborepo / Biome | 取り込み工程（`skill-matrix.tsx` の描画は全件正常） |
| D-2 (#140) | プロフィールの所属会社・性別・資格・得意分野・得意業務が画面に出ない。ただし原因は 2 種類ある。**所属会社**は `ProfileBlockData.company` と `profile-intro.tsx:26` の描画が既にあり、取り込みが値を入れていないだけ（トップバーの会社名も同じ理由で出ない）。**性別・資格・得意分野・得意業務**は `ProfileBlockData` に保存先そのものが無い。自己 PR も元データ 10 段落超から 1 段落へ圧縮されている | `packages/db/src/blocks.ts` `ProfileBlockData` / 取り込み工程 |
| D-3 (#139) | 会社概要文 `CompanyInfo.note` がビューア・PDF・Markdown・バックアップのどの出力経路にも描画先が無い。18 社分が消失し、バックアップからの復元でも失われる | `blocks.ts:95` に定義はあるが参照ゼロ |
| D-4 (#138) | 案件の `summary` / `duties` / `comment` が Markdown 未解釈のまま `<p>` に流し込まれ、箇条書き記号 `-` と半角スペースを含んだまま 1 段落に潰れ、`**強調**` が生表示される。同じデータが PDF では箇条書き・太字になるため **画面と PDF で表示が食い違う** | `project-card.tsx:46,81` |
| D-5 (#141) | 元データで「該当なし」を表すプレースホルダ `-` が 1 技術として扱われ、技術タグと技術フィルタに 9 件のチップとして出る | 取り込み時の正規化 / `process.ts` `flattenTech` |
| D-6 (#136) | 案件カードのチーム人数の単位が二重。DB 値が「13 名」「9 人」なのにテンプレートが「名」を無条件付与し「13 名名」「9 人名」となる。**全 32 件で発生** | `project-card.tsx:41` / `project-preview.tsx:151` |
| D-7 (#151) | 案件 21（PatentStart）の前置き一文が取り込み時に破棄されている | `migrate-real-sheet.ts` `parseCommentSection` |
| D-8 (#142) | 習熟度（★★★）が画面に一切出ない。`title` 属性のツールチップのみで、スマホでは到達不能。PDF には習熟度列があるため画面と PDF で情報量が違う | `skill-matrix.tsx:55` |

### レイアウト

| ID | 内容 | 主な箇所 |
|---|---|---|
| L-1 (#143) | 320px で案件カードのロール行・メタ行がカード外へ流出し、ページ全体に横スクロールが発生（`scrollWidth` 361 > 320）。375px でも余裕ゼロ | `project-card.tsx:38` の `shrink-0` |
| L-2 (#144) | 工程ステッパーのラベルが狭幅で崩壊。320px では「要/件/定/義」と 1 文字ずつ縦積みになり読めない。375px でも「実装」「・単体」に分裂して中黒が行頭に落ちる | `process-stepper.tsx:22-26` |
| L-3 (#150) | 320px でタイムラインの日付列（固定 `min-w-[132px]`）がタイトル列を約 100px まで圧迫し、案件名が 5〜6 行に断片化 | `timeline.tsx:35` |
| L-4 (#150) | 320px でビルダーのスキル表に `min-width` が無く、`overflow-x-auto` が効かずに列が潰れて経験年数が読めない | `builder-client.tsx:404-462` |

### 画面・導線

| ID | 内容 | 主な箇所 |
|---|---|---|
| U-1 (#148) | `not-found.tsx` が無く、404 が Next.js 既定の英語画面。ヘッダーも戻る導線も無く、ダークテーマ選択中でも純白のまま | `notFound()` は `view/[path]` `view/db/[id]` `compare` の 3 経路から呼ばれる |
| U-2 (#145) | 比較機能（`/compare`）が二重に壊れている。(1) 導線を持つ `sheets-list-client.tsx` はどこからも import されていない死にコードで、`/view` が描画するのは `db-sheets-list-client.tsx`。(2) `/compare` 自体が `isValidSheetPath()` / `getCachedSheet()` 経由の GitHub 由来 `.md` パスしか受け付けず、`/view` が並べる DB シートの ID を渡しても比較できない。導線を足すだけでは動かない | `app/view/page.tsx` / `app/compare/page.tsx` |
| U-3 (#149) | シート詳細から一覧 `/view` へ戻る導線が無い。トップバーの氏名は `<span>` でリンクではなく、唯一のリンクは `/builder` | `viewer-topbar.tsx:60-64` |
| U-4 (#149) | 閲覧者にも「編集／ビルダー」ペンアイコンが常時表示され、押すとログイン画面へ飛ばされる | `viewer-topbar.tsx:90-96` |
| U-5 (#151) | `/builder/preview` の直接アクセス時に「編集画面が閉じられました — 表示は最後の内容です」と出るが、実際は編集画面を開いてもいないし内容も無い。空状態の案内が無い | `preview-client.tsx:71` が未接続と切断を同一状態に潰している |
| U-6 (#155) | 閲覧ゲートが元の遷移先を保持しない。`/view/db/:id` を直リンクで開くと `/viewer-auth` へ飛ばされ、認証後は `/view` に着地して目的のシートへ戻れない。`/viewer-auth` 側は `?next=` に対応済みなのに、ゲートが付けていない | `viewer-gate.ts:29` の `redirect('/viewer-auth')` |
| U-7 (#157) | GitHub 連携未設定時の案内が `/view/[path]` と `/compare` で食い違う。`/compare` は「GitHub 連携が未設定のため…」と原因を出すのに、`/view/[path]` は「時間をおいて再度お試しください」と、待っても直らない原因に対して時間経過を促す文言を出す。`console.error` も 2 件出る | `view/[path]/page.tsx:41` が `throw err` で `app/error.tsx` に丸投げしている |

### PDF

| ID | 内容 | 主な箇所 |
|---|---|---|
| P-1 (#146) | 日本語の行末に不正なハイフンが入る（「バックエンド・デー-」「要-件定義」「現在-4 名」）。CJK を 1 文字ずつ返すハイフネーションコールバックを react-pdf がハイフン挿入位置として扱っている | `pdf/fonts.ts` `splitForHyphenation` |
| P-2 (#147) | 見出しと本体が改ページで分断される。「フロントエンド」がページ 1 末尾でスキル表がページ 2、「個人開発 — HorseManager」がページ 18 末尾で表がページ 19。直前のページ下部に大きな余白が残る | `pdf/skill-sheet-document.tsx` |

### 見た目

| ID | 内容 | 主な箇所 |
|---|---|---|
| S-1 (#152) | 担当工程ドーナツが `strokeLinecap="round"` のぶん一律 +15.1° 過大に描かれ、31/32 が満円（100%）に見える | `process-overview.tsx` |
| S-2 (#152) | 案件コメントに無条件 `italic` が当たり、和文 100〜918 字が全文合成斜体になる | `project-card.tsx:82` |
| S-3 (#152) | 統計カード 4 枚目「28 歳 / 年齢」が直上メタ行の「年齢 28 歳」と重複し、KPI の 1/4 が情報量ゼロ | シートデータ側 |
| S-4 (#152) | 技術フィルタの placeholder（ライト 3.35:1）とエラーバナーの文字（3.88:1）が WCAG AA 未達 | `tech-filter.tsx` / `bg-destructive/10` |
| S-5 (#152) | ビルダーの細部: ヘッダーで開閉トグルと別窓起動がともに「プレビュー」表記で判別できない／期間欄に「継続中」が 3 箇所重複しチェックボックスの余白が 0px／ドラッグハンドルが垂直中央で削除ボタンと非対称／検索アイコンだけ U+2315 のテキストグリフ | `builder-client.tsx` / `editor.css` |
| S-6 (#156) | キーボードフォーカスが見えない。ビューアで Tab を 12 回押して到達した要素のうち、フォーカスリングが出るのは shadcn 由来の 3 要素だけ。ビュートグル（`.softbtn`）・技術フィルタの入力・技術チップ（`.techtag`）は `outline` も `box-shadow` も付かない | `globals.css` の `.softbtn` / `.techtag` に `:focus-visible` 規則が無い |

### プロセス

| ID | 内容 |
|---|---|
| X-1 (#153) | `SETUP.md` のオーナーアカウント作成手順が `TODO（親エージェントが最終補記）` のまま。新規環境で `/login` を通せず、単一オーナー運用のブートストラップができない |
| X-2 (#153) | #113「Claude Design 準拠の全画面監査」は `createConsoleDemoSheet()` の合成デモシートで実施されており、実データ（32 案件）でしか出ない L-1 の横スクロールを取り逃していた。判定基準も「横スクロール無し + `console.error` ゼロ」のみで、表示内容の正しさを見ていない |

## 棄却した主な指摘

反証で落としたもののうち、判断が分かれそうなものを残す。

- ダークテーマでカード面とページ地のコントラストが 1.04:1 → `globals.css` に「Console: 影なし（ヘアライン境界のみ）」と設計意図が明記されており、意図的なトークン設計
- 「30+案件」と実件数 32 件の不一致 → `30+` は下限表記で 32 はこれを満たす。KPI はシート作成者の自己申告値で集計値ではない
- 一覧カードに氏名が出ない → 単一オーナー運用（`SKILLSHEET_OWNER_ID` で固定フィルタ）のため全カードが同一表記になり解決にならない
- ビュー全 OFF 時に空状態メッセージが無い → sticky なトグルが常時表示されており 1 クリックで復帰できる
- 会社名は匿名化されているのに製品名は実名 → 元データ側の記述であってアプリの逸脱ではない
- 空のテキスト（markdown）・職務経歴ブロックを追加して保存すると、リロードで消える（4 巡目に実測。DB のブロック数も増えない）→ `blocks.ts:373` の `isBlockInputEmpty` による意図的な drop。コメントに「保存時の drop と、全消し保存ガードで共有する単一の真実」と明記があり、`builder-client.tsx:221` は空ブロックを dirty 判定からも除外して「案件エディタタブを開いただけで幽霊ブロックができる」のを防いでいる。内容を入れれば残るので、データ損失ではない。ただし**追加した空ブロックが「保存しました」の後で黙って消える**点はユーザーから見ると驚きがあり、告知が無いのが妥当かは設計判断として残る（今回は逸脱に数えていない）
